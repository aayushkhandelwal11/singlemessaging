<%= form_for(@threadmessage) do |f| %>
  <% if @threadmessage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@threadmessage.errors.count, "error") %> prohibited this threadmessage from being saved:</h2>

      <ul>
      <% @threadmessage.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="assets">
    <% f.fields_for :assets do |asset| %>
       <%= asset.file_field :document %>
    <%end%>
  </div>
  <div id="addinglink">
  
  </div>
  <%= link_to 'New Attachment', new_asset_path %>
 
  <div class="actions">
    <%= f.submit 'send'%>
  </div>
<% end %>
