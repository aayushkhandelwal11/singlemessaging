<%= form_for @asset ,:html => { :multipart => true} do |f| %>
  <div class="field">
    <%= f.file_field :document %><br />
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', session[:return_to] %>
