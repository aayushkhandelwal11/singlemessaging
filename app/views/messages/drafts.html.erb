<h1>Listing Drafts</h1>
<%= paginate @messages %>
<table>
  <tr>
    <th>Content</th>
    <th> Date</th>
    <th></th>
    <th></th>
  </tr>
<% @messages.each do |message| %>
  <tr> 
    <td><%= message.subject %></td>
    <td><%= message.updated_at.ctime %></td>
    <td><%= link_to 'edit' ,edit_message_path(message.id)%></td>
    <td><%= link_to "Delete", messages_url({:id => message.id}) %></td>
  </tr>
<% end %>
</table>
<br />
